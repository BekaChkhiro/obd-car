# Absolute origin, no trailing slash. Drives canonical URLs, hreflang
# alternates, sitemap entries, Open Graph URLs and JSON-LD @id values.
NEXT_PUBLIC_SITE_URL=https://obdcar.ge

# Set once the store listings exist — the hero swaps its CTA for real badges.
NEXT_PUBLIC_APP_STORE_URL=
NEXT_PUBLIC_PLAY_STORE_URL=
